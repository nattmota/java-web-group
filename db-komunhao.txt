CREATE DATABASE db-lv 
USE db-komunhao

CREATE TABLE Livro (
    idLivro INT AUTO_INCREMENT PRIMARY KEY,
    titulo VARCHAR(255),
    autor VARCHAR(255),
    preco FLOAT(10, 2)
);

CREATE TABLE Carrinho (
    idCarrinho INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT,
    id_livro INT,
    quantidade INT,
    FOREIGN KEY (id_usuario) REFERENCES Usuarios(id),
    FOREIGN KEY (id_livro) REFERENCES Livros(id)
);

CREATE TABLE Usuario (
   idUsuario INT AUTO_INCREMENT PRIMARY KEY,
   nome VARCHAR(255),
   email VARCHAR(255) UNIQUE,
   senha VARCHAR(255),
   cpf VARCHAR(14),
   telefone VARCHAR(15)
);

INSERT INTO livro (titulo, autor, preco) VALUES ('O Senhor dos Anéis', 'J.R.R. Tolkien', 39.99);
INSERT INTO livro (titulo, autor, preco) VALUES ('Harry Potter e a Pedra Filosofal', 'J.K. Rowling', 29.90);
INSERT INTO livro (titulo, autor, preco) VALUES ('1984', 'George Orwell', 25.50);
INSERT INTO livro (titulo, autor, preco) VALUES ('A Menina que Roubava Livros', 'Markus Zusak', 34.75);
INSERT INTO livro (titulo, autor, preco) VALUES ('O Código Da Vinci', 'Dan Brown', 42.00);

